namespace KeyGen{
	// input - the name of the register
	// outpit - output parameter to receive the result
	// Return 0 on success
	size_t generate(TCHAR input[], size_t inputLength, TCHAR output[], size_t outputLength);
}